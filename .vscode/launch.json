{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Unit tests",
            "program": "${workspaceFolder}/node_modules/.bin/jest",
            "outFiles": [
                "${workspaceFolder}/**/*.js"
            ]
        }
    ]
}